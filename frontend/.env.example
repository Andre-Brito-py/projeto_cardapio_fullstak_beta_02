# Configuração de URLs para o Frontend (Cliente)
# Copie este arquivo para .env e ajuste as URLs conforme necessário

# URLs de Desenvolvimento (padrão)
VITE_FRONTEND_URL=http://localhost:5174
VITE_ADMIN_URL=http://localhost:5173
VITE_BACKEND_URL=http://localhost:4000

# URLs de Produção (configure quando fazer deploy)
# VITE_FRONTEND_URL=https://your-frontend-domain.com
# VITE_ADMIN_URL=https://your-admin-domain.com
# VITE_BACKEND_URL=https://your-backend-domain.com

# Configurações do Vite
VITE_NODE_ENV=development

# Configurações de Pagamento (opcional)
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key

# IMPORTANTE:
# - VITE_FRONTEND_URL: URL onde roda a aplicação do cliente e garçom (padrão: porta 5174)
# - VITE_ADMIN_URL: URL onde roda o painel administrativo (padrão: porta 5173)
# - VITE_BACKEND_URL: URL da API do backend (padrão: porta 4000)
# 
# No Vite, todas as variáveis de ambiente devem começar com VITE_ para serem acessíveis no frontend
# Certifique-se de que as portas estão corretas para evitar problemas com links!